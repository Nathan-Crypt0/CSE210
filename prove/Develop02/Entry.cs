using System;

class Program
{ 
    static void Main(string[] args)
    {
       Console.WriteLine("Please pick a menu option. Enter the number to pick.") 
       string menuChoice = Console.ReadLine();

        string menuAnswer = "";
       
       if (menuChoice = 1) 
       (menuAnswer = Console.WriteLine("1. Write to journal."))

        else if (menuChoice = 2) 
        (menuAnswer = Console.WriteLine("2. Display old entries. "))

        else if (menuChoice = 3) 
        (menuAnswer = Console.WriteLine("3. Save journal to file"))
        
        else if (menuChoice = 4) 
        (menuAnswer = Console.WriteLine("4. Load load journal to file"))


    }
}